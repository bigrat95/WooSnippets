/******************************************************************
***Limit the display to 20 products in woocommerce archive pages***
*******************************************************************/ 
add_filter( 'loop_shop_per_page', 'new_loop_shop_per_page', 20 );

function new_loop_shop_per_page( $cols ) {

  $cols = 20;
  return $cols;
}
